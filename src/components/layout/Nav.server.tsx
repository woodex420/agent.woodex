/** Linoxa-era: the LinoxaHeader handles its own static nav (mega-menu on Services). */
import LinoxaHeader from "@/components/linoxa/LinoxaHeader";

export default async function NavServer() {
  return <LinoxaHeader />;
}
